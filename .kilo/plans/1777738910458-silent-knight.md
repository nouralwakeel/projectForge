# Plan: Replace Projects Tab with Teams Discovery (المطابقة) in Bottom Nav

## Summary
Replace the "المشاريع" (Projects) tab in the bottom navigation bar of `HomeScreen` with a "المطابقة" (Teams Matching) tab that displays the team discovery UI from the provided HTML design, converted to Flutter.

## Changes Required

### 1. Update Bottom Nav Bar in `home_screen.dart`
**File:** `projectforge_app/lib/views/dashboard/home_screen.dart`

- Replace the 4-tab nav items `[الرئيسية, المشاريع, المهام, الإعدادات]` with the HTML design's nav items:
  - `الرئيسية` (Home) — `dashboard` icon
  - `المطابقة` (Matching) — `groups` icon (active/filled when selected)
  - `المختبر` (Lab) — `biotech` icon
  - `الملف` (Profile) — `person` icon
- Update `_buildPlaceholderPage` to handle the new tab indices (1=Matching, 2=Lab, 3=Profile)
- When index 1 (المطابقة) is selected, show the new `_TeamsDiscoveryPage` widget instead of a placeholder
- When index 3 (الملف) is selected, navigate to or show the profile
- Update active tab styling to match the HTML design: pink background highlight with filled icon

### 2. Create `_TeamsDiscoveryPage` Widget inside `home_screen.dart`
This widget will convert the HTML team discovery page into Flutter, including:

#### a. Header Section
- Title: "اكتشف فريقك المثالي"
- Subtitle: "استكشف الفرق التي تبحث عن مهاراتك وانضم إلى المشروع الأنسب لك."
- Filter chips row (horizontally scrollable): الكل, الذكاء الاصطناعي, تطوير الويب, تطبيقات الجوال

#### b. Top Match Highlight (Bento Grid)
- Left large card: Featured team with match percentage circle (95%), team name, description, skill tags, and "طلب انضمام" button
- Right card: "مهاراتك المطلوبة" with progress bars showing Flutter (80%), UI/UX Design (50%), Firebase (30%)

#### c. Suggested Teams Grid
- Section title: "فرق أخرى مقترحة"
- Grid of team cards (responsive: 1 col mobile, 2 cols tablet, 3 cols desktop) each with:
  - Color accent bar at top
  - Team name + match percentage badge
  - Description text
  - Required skills as chips
  - Member avatars row + "التفاصيل" link
  - The data will be pulled from `TeamController.teams` via GetX

#### d. Styling
- Use `AppTheme` colors throughout (primary, tertiary, secondary containers, etc.)
- RTL layout via `Directionality`
- Responsive grid using `LayoutBuilder` or `GridView`
- Glassmorphism/gradient effects for the hero card matching the HTML design

### 3. Update Tab Navigation Logic
- `_currentIndex == 0` → `_OverviewPage` (existing home overview)
- `_currentIndex == 1` → `_TeamsDiscoveryPage` (new)
- `_currentIndex == 2` → Lab placeholder or navigate to sandbox
- `_currentIndex == 3` → Navigate to `AppRoutes.profile`

### 4. Data Integration
- `_TeamsDiscoveryPage` will use `TeamController` from GetX to fetch and display teams
- Match percentages will come from team data or recommendation data
- The "skill demand" section can use hardcoded mock data initially, or pull from recommendation controller

### 5. No Changes Needed To
- `app_routes.dart` — routes stay the same
- `app_pages.dart` — pages stay the same
- `team_controller.dart` — controller stays the same
- `team_model.dart` — model stays the same
- Bottom nav styling is updated in place within `home_screen.dart`

## Files Modified
1. `projectforge_app/lib/views/dashboard/home_screen.dart` — Major update: new bottom nav tabs, new `_TeamsDiscoveryPage` widget with all sub-widgets